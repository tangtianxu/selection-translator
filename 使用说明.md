# 划词翻译（ttx的vibe coding小工具）

这是一个 Windows 轻量翻译工具，支持跨应用选中文字翻译和剪贴板自动翻译。

## 使用方法

1. 双击 `划词翻译.exe` 启动。
2. 在浏览器、PDF、Word 或其他软件中选中文字。
3. 按 `Ctrl+Shift+T`，程序会复制所选文字并弹出译文。

也可以勾选“剪贴板变化时自动翻译”。开启后，在任意软件中复制文字，翻译窗口会自动出现。

## 功能

- 自动检测原文语言。
- 可点击程序顶部的“设置…”并直接按下自定义快捷键，设置会自动保存。支持 `Ctrl / Alt / Shift / Win` 搭配字母、数字或 `F1–F12`；组合中至少需要包含 `Ctrl`、`Alt` 或 `Win`。
- 可勾选“中英互翻”：检测为中文时翻译成英文，检测为英文时翻译成中文。
- 可勾选“窗口保持置顶”，让翻译结果始终显示在其他窗口上方。
- “原文”右侧可收起或展开原文区域，收起后译文区域会获得更多空间。
- “译文”右侧可开启“仅译文”模式，划词弹窗会缩为只显示译文的紧凑界面。
- “自动公式”开启后会识别 LaTeX 行内或独立公式，支持 `$...$`、`\(...\)`、`\[...\]`，也能判断包含 LaTeX 命令或上下标的普通括号公式 `( ... )`。Google 翻译时会保护公式内容，并直接在译文区域显示排版结果。底部状态栏会显示成功和失败数量；不受支持的公式会以红色源码保留，不会丢失内容。
- 不开启自动公式时，可点击“公式转换”在右侧面板按需查看结果；再次点击即可收起，在源码译文与转换结果之间自由查看。
- 自动保存最近 200 条成功翻译记录，可按原文、译文、语言、模式或时间搜索。
- 历史记录以主窗口右侧的可折叠面板显示，展开后左侧为原文、右侧为翻译结果；再次点击“历史记录”即可收起。下方完整译文会跟随主界面的“自动公式”开关，也可用“公式转换 / 显示源码”单独切换。支持查看完整内容、重新载入、复制译文、删除单条记录和清空全部记录。
- 展开历史面板时只增加窗口总宽度，不压缩原翻译区域或遮挡按钮。
- 可翻译为简体中文、英语、日语、韩语、法语、德语、西班牙语和俄语。
- `Ctrl+Enter` 可翻译输入框中的内容。
- 可一键复制译文。
- 关闭程序前，最小化窗口不会影响全局快捷键。

## 注意事项

- 翻译需要联网。
- 当前使用 Google 翻译的免密钥在线接口，适合个人轻量使用，不建议用于敏感、保密或批量文本。
- 翻译历史保存在当前 Windows 用户的本机应用数据目录中；敏感内容可在历史窗口中及时删除。
- 单次最多翻译 5000 个字符。
- 某些以管理员权限运行的软件可能阻止普通权限程序模拟复制；此时可手动按 `Ctrl+C`，并开启剪贴板自动翻译。
- 如果所选快捷键被其他软件占用，程序底部会提示，可换一个快捷键或改用剪贴板模式。
- 点击窗口右上角关闭按钮会隐藏到 Windows 系统托盘；双击托盘图标可恢复窗口，右键托盘图标可选择“显示翻译窗口”或“退出程序”。界面中的“退出”按钮也可完全关闭。
- 窗口隐藏到托盘后，剪贴板变化不会自动弹出窗口；仍可使用全局快捷键主动唤起翻译。
- 未开启“复制后自动翻译”时，全局快捷键会临时读取选中文字并立即恢复原剪贴板；开启后则保留本次复制的文字。常见文本、HTML、富文本、文件和图片格式会尽量一并恢复。
- 程序只读取经过确认可安全复制的剪贴板格式，未知的应用私有格式会跳过，避免因错误处理原生句柄而崩溃。
- 公式优先完全在本地渲染。程序会自动兼容 Markdown 转义的下划线，以及 `\mathbf c`、`\mathcal C`、`\mathsf T` 等未加大括号的常见 TeX 简写；转换只用于显示，不会修改复制内容或历史记录。常见的上下标、分式、根式、求和、积分、极限、希腊字母和概率公式均受支持；矩阵、分段函数及多行对齐等完整 LaTeX 环境可能保留为红色源码。当前未启用在线公式渲染，避免额外上传文本和增加网络依赖。
- 点击主界面的“日志”，或右键系统托盘图标选择“打开日志”，可以查看运行记录。普通错误写入 `app.log`，原生致命错误的 Python 线程状态写入 `fatal.log`。

## 源码运行

本机安装 Python 3 后，可运行：

```powershell
python SelectionTranslator.py
```
